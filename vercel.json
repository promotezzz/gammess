{
    "version": 2,
    "builds": [
        { "src": "server.js", "use": "@vercel/node" }
    ],
    "routes": [
        { "src": "/bare/(.*)", "dest": "server.js" },
        { "src": "/(.*)", "continue": true }
    ]
}
